import * as React from 'react';

interface ISplashProps {
}

const Splash: React.SFC<ISplashProps> = (props: ISplashProps) => {
  return (
    <div className="Splash">
      <p>
        Splash
      </p>
    </div>
  );
};

export default Splash;

